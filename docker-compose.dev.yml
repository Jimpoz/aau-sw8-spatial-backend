services:
  middleware:
    volumes:
      - ./middleware:/app

  backend:
    ports:
      - "8000:8000"

  assistant:
    ports:
      - "8001:8001"

  image_pipeline:
    ports:
      - "8002:8002"
